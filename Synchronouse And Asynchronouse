synchronouse VS Asynchronouse: JavaScript is a single threaded, which means that it executes only one thread at a time and after completion of that starts next thread. Same is called synchronouse.synchronous code will block further execution of the remaining code until it finishes the current one. While in Asysnchronouse every block of code executing independently and not waiting for one another.
